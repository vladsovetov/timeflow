/** Android notification channel (matches expo-notifications plugin defaultChannel). */
export const ACTIVE_TIMER_CHANNEL_ID = "active-timer";

/**
 * Category for Pause action. Expo forbids `:` and `-` in category identifiers.
 */
export const ACTIVE_TIMER_CATEGORY_ID = "active_timer";

export const ACTIVE_TIMER_PAUSE_ACTION_ID = "pause";

export const ACTIVE_TIMER_NOTIFICATION_ID = "timeflow-active-timer";
